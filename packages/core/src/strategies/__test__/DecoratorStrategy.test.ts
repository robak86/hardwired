import { DecoratorStrategy } from '../DecoratorStrategy';

describe(`DecoratorStrategy`, () => {
  describe(`it`, () => {
    it.skip(`inherits the same id as decorated resolver`, async () => {
      // const resolver1 = singleton(() => 1);
      // const decoratorResolver = new DecoratorStrategy<any, any>(resolver1, val => val + 1);
      // expect(decoratorResolver.id).toEqual(resolver1.id);
    });
  });
});
