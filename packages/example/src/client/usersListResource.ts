import { createCommandResource } from '@roro/resource';
import { createUserRoute } from '../contract/usersContract';

// // defined at the client side
// export const usersListResource = createCommandResource({
//   createUserRoute,
//   mount: null as any,
// });
